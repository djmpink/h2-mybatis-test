<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.teamstack.repository.LogMapper">

    <resultMap id="resultMap" type="cn.teamstack.entity.LogConfig">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="path" property="path"/>
    </resultMap>

    <select id="findById" resultMap="resultMap">
        SELECT
            id,
            name,
            path
        FROM T_LOG_CONFIG
        WHERE
            id = #{id}
    </select>
    <select id="find" resultMap="resultMap">
        SELECT
            id,
            log_id,
            IP,
            name,
            path,
            type,
            tags,
            createUserId,
            modifyUserId,
            createTime,
            modifyTime,
            isValid
        FROM T_LOG_CONFIG WHERE 1=1
        ORDER BY createTime DESC
        LIMIT #{configReq.offset},
        #{configReq.pageSize}
    </select>
    <select id="count" resultType="java.lang.Long">
        SELECT count(1)
        FROM T_LOG_CONFIG;
    </select>
    <insert id="insert">
        INSERT INTO T_LOG_CONFIG
        (
            log_id,
            IP,
            name,
            path,
            type,
            tags,
            createUserId,
            modifyUserId,
            createTime,
            modifyTime,
            isValid
        )
        VALUES
            (
                #{logConfig.logId},
                #{logConfig.IP},
                #{logConfig.name},
                #{logConfig.path},
                #{logConfig.type},
                #{logConfig.tags},
                #{logConfig.createUserId},
                #{logConfig.modifyUserId},
                #{logConfig.createTime},
                #{logConfig.modifyTime},
                #{logConfig.isValid}
            )
    </insert>
</mapper>